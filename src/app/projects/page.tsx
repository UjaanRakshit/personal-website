import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "projects",
  description: "Selected systems, GPU, and quant work by Ujaan Rakshit.",
};

export default function ProjectsRoute() {
  return null;
}
